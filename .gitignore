# Secrets - NIEMALS committen
.env
*.env
!.env.example

# Daten (lokaler Start legt ./data an)
data/
claimer-data/
.venv/
*.db
*.db-wal
*.db-shm
backups/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# Node
node_modules/
.vite/
*.tsbuildinfo

# frontend/dist wird BEWUSST mitgeliefert: so genuegt Python zum Starten,
# Node.js ist nur zum Neubauen noetig (python run.py --rebuild).
# Andere dist-Ordner bleiben draussen.
/dist/
backend/dist/

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Live-Fixtures (koennen personalisierte Daten enthalten)
backend/tests/fixtures/live/
